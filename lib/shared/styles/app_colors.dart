import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF448AFF);
  static const Color background = Color(0xFFF2F4F7);
  static const Color surface = Colors.white;

  static const Color bubbleUser = Color(0xFF448AFF);
  static const Color bubbleAi = Colors.white;

  static const Color textUser = Colors.white;
  static const Color textAi = Color(0xFF1F2937);

  static const Color textSecondary = Color(0xFF9CA3AF);

  static Color timestampUser = Colors.white.withOpacity(0.7);
  static Color timestampAi = Colors.grey.withOpacity(0.8);

  static const Color iconActive = Color(0xFF448AFF);
  static const Color iconInactive = Colors.grey;
}